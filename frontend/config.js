// Edit this one line after deploying the backend — nothing else in the
// frontend needs to change. Leave as-is for local development.
window.WORLDSHINER_API_BASE = 'https://worldshiner2-api.onrender.com/api';
